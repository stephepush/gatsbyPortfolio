import React from "react";

const Contacts = () => {
    return (
        <section class="portfolio-sections" id="contact">
            <h2>Reach Out</h2>
        </section>
    )
}

export default Contacts;